spring:
  application:
    name: pgf-backend
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/pgf_db}
    username: ${SPRING_DATASOURCE_USERNAME:pgf_user}
    password: ${SPRING_DATASOURCE_PASSWORD:rescue}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true

app:
  upload:
    dir: ${USER_HOME:${user.home}}/pgf-uploads
    max-size: 10485760  # 10MB

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.pgf: INFO

---
# Profil Supabase
spring:
  config:
    activate:
      on-profile: supabase
  datasource:
    url: ${SUPABASE_DATABASE_URL:jdbc:postgresql://db.bhjpavcxhymxcadesnqy.supabase.co:5432/postgres?sslmode=require}
    username: ${SUPABASE_DB_USERNAME:postgres}
    password: ${SUPABASE_DB_PASSWORD:BcxnBstrBYr6Tx92}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

supabase:
  url: ${SUPABASE_URL:https://bhjpavcxhymxcadesnqy.supabase.co}
  service-key: ${SUPABASE_SERVICE_KEY:}

app:
  upload:
    dir: ${java.io.tmpdir}/pgf-uploads