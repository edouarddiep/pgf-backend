spring:
  application:
    name: pgf-backend
  profiles:
    active: supabase
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB
      enabled: true

server:
  port: 8080

supabase:
  url: ${SUPABASE_URL:https://bhjpavcxhymxcadesnqy.supabase.co}
  service-key: ${SUPABASE_SERVICE_KEY:eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImJoanBhdmN4aHlteGNhZGVzbnF5Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc1NzI2NDkyNiwiZXhwIjoyMDcyODQwOTI2fQ.AKh5pH-O9LHGtAzNl-WsGuqGlI8485AGKV_4-tK1Fo4}

app:
  upload:
    supabase:
      bucket: "oeuvres"
      # Optimisations d'images
      max-width: 1200
      max-height: 800
      jpeg-quality: 0.85
      thumbnail-size: 300

  admin:
    password: ${ADMIN_PASSWORD:pgf-admin-2025}

---
spring:
  config:
    activate:
      on-profile: supabase
  datasource:
    url: ${SUPABASE_DATABASE_URL:jdbc:postgresql://db.bhjpavcxhymxcadesnqy.supabase.co:5432/postgres?sslmode=require}
    username: ${SUPABASE_DB_USERNAME:postgres}
    password: ${SUPABASE_DB_PASSWORD:A2NFWaEvE4QS79JF}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true